server:
  port: 8888

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/kaidianmiao?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: cptbtptp
  data:
    redis:
      host: localhost
      port: 6379
      database: 0

wechat:
  appid: wx6a8a2d8208c42603
  secret: ${WECHAT_APP_SECRET:}

jwt:
  secret: ${JWT_SECRET:kaidianmiao-jwt-secret-key-2026}
  expiration: 604800000  # 7 days in milliseconds

logging:
  level:
    com.kaidianmiao: DEBUG
    org.springframework.web: DEBUG